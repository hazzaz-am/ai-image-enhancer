export const enhancedImageAPI = async (file) => {
	try {
    //!TODO: upload image = /api/tasks/visual/scale

    //!TODO: enhance image = /api/tasks/visual/scale/{task_id}
	} catch (error) {
		console.log(error);
	}
};
